# --- Database (cPanel MySQL) ---
DB_HOST=localhost
DB_PORT=3306
DB_USER=polarisAdmin
DB_PASSWORD=Polaris6432
DB_NAME=polaris

# --- Server ---
# Port assigned by cPanel Node.js selector
PORT=3016

# --- Auth (better-auth) ---
BETTER_AUTH_SECRET=change-me-to-a-long-random-string
# Single-domain deployment: the Node app serves both the API (/api) and the
# built frontend (public/) on the same origin — no API subdomain.
BETTER_AUTH_URL=https://polarisstyle.ir
FRONTEND_URL=https://polarisstyle.ir
